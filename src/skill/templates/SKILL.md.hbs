---
name: {{skillName}}
description: '{{description}}'
argument-hint: '{{{argumentHint}}}'
---

# {{title}}

> **🤖 AI 必读**：本 Skill 由 toolcapsule 自动生成，已包含从 MCP 服务器获取的完整工具列表和参数骨架。
> **首次使用时**，请完成以下补全工作（仅此一次）：
> 1. 将已验证可用的调用参数替换下方「常用示例」中的占位值
> 2. 根据工具列表和实际体验，填写「适用场景」
>
> **补全之后，后续所有调用直接参考本文件即可，无需再查询 MCP。**

## 适用场景

（AI 首次使用时根据下方工具列表自行填写）

## 工具列表

以下列表由 toolcapsule 直接从 MCP 服务器获取。每个工具附带了参数骨架（必填参数在骨架中，可选参数已注释）。

{{{toolsMarkdown}}}

## 使用方法

```bash
# 查看所有工具（简要）
tcap tools {{profileName}} --brief

# 查看单个工具的完整参数说明
tcap schema {{profileName}} <工具名>

# 调用工具（参数写入本地 JSON 文件）
tcap call {{profileName}} <工具名> @args.json --save-run

# 修改参数文件后重试
tcap retry .toolcapsule/runs/{{profileName}}/<run-id>
```

## 常用示例

（AI 首次使用时，将已验证可用的调用参数替换下方占位值。每个工具保留一个示例。）

{{{examplesMarkdown}}}

## 安全提醒

- 不要编造文档 ID、URL、用户 ID 等标识符
- 大文档内容通过本地文件传递，避免撑满上下文
- 不要打印或提交包含鉴权信息的 URL
- 调用前确认操作的影响范围
